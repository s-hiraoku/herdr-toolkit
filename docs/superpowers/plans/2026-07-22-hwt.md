# hwt（herdr worktree ラッパー）Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** `dispatch` プラグインを、git worktree を短いコマンドで扱う `hwt` に改称・サブコマンド化し、`herdr-toolkit` を複数プラグイン拡張しやすい構造にする。

**Architecture:** 実体は 1 本の bash スクリプト `plugins/hwt/hwt.sh`。先頭で `case "$1"` の verb ディスパッチを行い各 verb を関数（`cmd_new`/`cmd_ls`/`cmd_cd`/`cmd_clean`/`cmd_rm`）に振る。CLI と herdr plugin action の両方から同じスクリプトが呼ばれる。内部は `herdr worktree`/`workspace`/`agent` を叩く薄いラッパー。

**Tech Stack:** bash（macOS 同梱 3.2 互換）、`jq`、`git worktree`、herdr CLI 0.7.5。

## Global Constraints

- 対象リポジトリ: `s-hiraoku/herdr-toolkit`（実装は `~/ghq/github.com/s-hiraoku/herdr-toolkit`）。
- bash 3.2 互換: `$BASHPID` など bash 4+ 機能を使わない。`set -eu` 前提で unbound 変数に注意。
- ブランチ接頭辞は `hwt/*`（旧 `dispatch/*` は使わない）。
- worktree パスは herdr の `[worktrees].directory`（既定 `~/.herdr/worktrees`）に委ねる。
- agent 名は herdr 命名規約 `[a-z][a-z0-9_-]{0,31}`（先頭小文字・小文字/数字/-/_・最大32）に正規化。
- herdr `--json` は JSON 前に `ok` 等の行を出す → `grep -m1 '^{'` で抽出。
- worktree パスは `.result.workspace.worktree.checkout_path`（旧 `.result.worktree.path` もフォールバック）。
- plugin action の `$PWD` はプラグイン root。対象 repo は `HERDR_ACTIVE_PANE_CWD` →
  `HERDR_PLUGIN_CONTEXT_JSON.focused_pane_cwd` → `$PWD` の順で解決。
- `git rev-list --exclude` のパターンは `refs/heads/` を除いた短い名前で渡す（付けると誤削除）。
- 環境変数は `HWT_AGENT`（既定 `claude`）、`HWT_MAX_WATCHERS`（既定 16）。
- 前提: 先に PR #3（README 分割・A）をマージし、更新済み `main` からブランチを切る。
- コミットは Conventional Commits。各タスク末尾でコミット。
- 検証は一時 git リポジトリ＋稼働中 herdr に対する E2E。テスト用に作った worktree/workspace は
  各検証の最後に必ず `herdr worktree remove --workspace <id> --force` と `rm -rf` で掃除する。

---

## File Structure

- Create: `plugins/hwt/hwt.sh` — 実装本体（`plugins/dispatch/dispatch.sh` を `git mv` して全面改修）
- Modify→Rename: `plugins/hwt/herdr-plugin.toml`（旧 `plugins/dispatch/herdr-plugin.toml`）— id/actions/version
- Rewrite: `plugins/hwt/README.md` — verb 表記のプラグイン doc
- Modify: `config/config.toml` — キーバインドを hwt の action id へ
- Modify: `scripts/setup.sh` — `plugins/*` 汎用リンク＋CLI を `~/.local/bin` へ symlink
- Modify: `README.md`（トップ）— プラグインリンクを hwt に更新

---

### Task 1: プラグインの改称と manifest

**Files:**
- Rename: `plugins/dispatch/` → `plugins/hwt/`、`dispatch.sh` → `hwt.sh`
- Modify: `plugins/hwt/herdr-plugin.toml`

- [ ] **Step 1: ディレクトリとスクリプトを git mv**

```bash
cd ~/ghq/github.com/s-hiraoku/herdr-toolkit
git mv plugins/dispatch plugins/hwt
git mv plugins/hwt/dispatch.sh plugins/hwt/hwt.sh
```

- [ ] **Step 2: herdr-plugin.toml を書き換え**

`plugins/hwt/herdr-plugin.toml` を以下の内容にする:

```toml
id = "s-hiraoku.hwt"
name = "hwt"
version = "1.0.0"
min_herdr_version = "0.7.5"
description = "Ergonomic wrapper over herdr worktree: create/list/switch/clean/remove git worktrees with short commands; optionally launch a coding agent inside."
platforms = ["linux", "macos"]

[[actions]]
id = "new"
title = "New worktree (no agent)"
command = ["bash", "hwt.sh", "new"]

[[actions]]
id = "new-agent"
title = "New worktree + launch agent"
command = ["bash", "hwt.sh", "new", "-a"]

[[actions]]
id = "ls"
title = "List hwt worktrees"
command = ["bash", "hwt.sh", "ls"]

[[actions]]
id = "cd"
title = "Switch to a worktree workspace"
command = ["bash", "hwt.sh", "cd"]

[[actions]]
id = "clean"
title = "Clean up hwt worktrees (safe: keeps changed ones)"
command = ["bash", "hwt.sh", "clean"]

[[actions]]
id = "rm"
title = "Remove the current worktree (confirm)"
command = ["bash", "hwt.sh", "rm"]
```

- [ ] **Step 3: 旧プラグインを unlink し新プラグインを link**

```bash
herdr plugin unlink s-hiraoku.dispatch 2>/dev/null || herdr plugin uninstall s-hiraoku.dispatch 2>/dev/null || true
herdr plugin link ~/ghq/github.com/s-hiraoku/herdr-toolkit/plugins/hwt
```

- [ ] **Step 4: 登録を検証**

Run: `herdr plugin list | grep -i hwt`
Expected: `s-hiraoku.hwt (hwt) enabled [local:.../plugins/hwt]`

Run: `herdr plugin action invoke ls --plugin s-hiraoku.hwt >/dev/null 2>&1; echo $?`
Expected: `0`（action が存在し呼べる。中身の実装は後続タスク。この時点では旧 dispatch.sh の
挙動なので出力は問わない）

- [ ] **Step 5: Commit**

```bash
git add plugins/hwt/herdr-plugin.toml
git commit -m "refactor(hwt): dispatch を hwt に改称し manifest を verb アクションに"
```

---

### Task 2: hwt.sh — スケルトン（dispatch＋共有ヘルパ＋`new`）

`plugins/hwt/hwt.sh` を**全面的に書き換える**。この Task では共有部分と `new` verb を実装し、
他 verb（ls/cd/clean/rm）は後続 Task で関数を追加していく（dispatch の case にも順次足す）。

**Interfaces（後続 Task が依存する共有関数）:**
- `resolve_cwd()` → 対象 repo の cwd を stdout に返す
- `slugify(text)` → ブランチ用 slug を stdout に返す
- `notify(msg [,sound])` → herdr 通知（失敗しても無視）
- `watch_agent(name)` → 完了監視をバックグラウンド起動＋pidfile 登録
- `start_agent_in_pane(name, pane_id, prompt)` → 既存ペインに agent 起動（リトライ付き）
- 末尾の verb ディスパッチ `case "$verb"` に各 Task が 1 行ずつ追加する

- [ ] **Step 1: hwt.sh を以下の内容で全置換**

```bash
#!/usr/bin/env bash
# hwt.sh — herdr worktree を楽に扱う CLI / herdr プラグイン本体
#
# 使い方:
#   hwt new [-n N] [テキスト]        # 新規 worktree+workspace を作成(agent なし)
#   hwt new -a [-n N] [テキスト]     # 新規 worktree+workspace + agent 起動
#   hwt ls                          # 現 repo の hwt/* worktree を一覧
#   hwt cd                          # worktree の workspace を選んでフォーカス移動
#   hwt clean                       # hwt/* の残骸を安全に掃除(変更ありは保護)
#   hwt rm                          # 今いる worktree を確認つきで破棄
#
# 環境:
#   plugin action/カスタムコマンドからは HERDR_ACTIVE_PANE_CWD / HERDR_PLUGIN_CONTEXT_JSON が
#   渡される。CLI 直接実行では $PWD を使う。エージェントは HWT_AGENT(既定 claude)で差し替え可能。
set -eu

# plugin action は最小 PATH で走るため herdr/jq を補完。herdr 本体は HERDR_BIN_PATH で解決。
if [ -n "${HERDR_BIN_PATH:-}" ]; then
  PATH="$(dirname "$HERDR_BIN_PATH"):$PATH"
fi
PATH="$PATH:/opt/homebrew/bin:/usr/local/bin:/usr/bin:/bin"
export PATH

AGENT_CMD="${HWT_AGENT:-claude}"
WATCH_DIR="${HERDR_PLUGIN_STATE_DIR:-${TMPDIR:-/tmp}/herdr-hwt}/watchers"
MAX_WATCHERS="${HWT_MAX_WATCHERS:-16}"
case "$MAX_WATCHERS" in ''|*[!0-9]*) MAX_WATCHERS=16 ;; esac

notify() { herdr notification show "$1" ${2:+--sound "$2"} >/dev/null 2>&1 || true; }

# 対象 repo の cwd を解決（plugin action の $PWD はプラグイン root なので使わない）
resolve_cwd() {
  local c="${HERDR_ACTIVE_PANE_CWD:-}"
  if [ -z "$c" ] && [ -n "${HERDR_PLUGIN_CONTEXT_JSON:-}" ]; then
    c="$(printf '%s' "$HERDR_PLUGIN_CONTEXT_JSON" | jq -r '.focused_pane_cwd // .workspace_cwd // empty' 2>/dev/null || true)"
  fi
  [ -z "$c" ] && c="$PWD"
  printf '%s' "$c"
}

slugify() { printf '%s' "$1" | tr -c 'a-zA-Z0-9' '-' | tr -s '-' | sed 's/^-//; s/-$//' | cut -c1-24; }

# ---- 完了ウォッチャ(バックグラウンド・ポーリング) ----
prune_dead_watchers() {
  [ -d "$WATCH_DIR" ] || return 0
  local pf pid
  for pf in "$WATCH_DIR"/*.pid; do
    [ -e "$pf" ] || continue
    pid="$(cat "$pf" 2>/dev/null || true)"
    if [ -z "$pid" ] || ! kill -0 "$pid" 2>/dev/null; then rm -f "$pf"; fi
  done
}

live_watcher_count() {
  local n=0 pf
  for pf in "$WATCH_DIR"/*.pid; do [ -e "$pf" ] && n=$((n + 1)); done
  echo "$n"
}

watch_agent() {
  local target="$1"
  if ! mkdir -p "$WATCH_DIR" 2>/dev/null; then
    echo "⚠️ watcher 状態ディレクトリを作成できず $target の完了通知はスキップします ($WATCH_DIR)" >&2
    return 0
  fi
  prune_dead_watchers
  if [ "$(live_watcher_count)" -ge "$MAX_WATCHERS" ]; then
    echo "⚠️ 完了監視が上限(${MAX_WATCHERS}件)に達したため $target の通知はスキップします" >&2
    return 0
  fi
  (
    local deadline=$(( $(date +%s) + 7200 ))
    sleep 30
    while [ "$(date +%s)" -lt "$deadline" ]; do
      local out status
      if ! out="$(herdr agent get "$target" 2>/dev/null)"; then
        notify "hwt: $target が終了しました (pane クローズ)" done
        return
      fi
      status="$(printf '%s' "$out" | grep '^{' | jq -r '.result.agent.agent_status // .result.agent_status // "unknown"' 2>/dev/null || echo unknown)"
      case "$status" in
        idle)    notify "hwt: $target が完了 (idle)" done; return ;;
        blocked) notify "hwt: $target が入力待ち (blocked)" request; return ;;
      esac
      sleep 20
    done
    notify "hwt: $target の監視が2時間でタイムアウトしました" request
  ) >/dev/null 2>&1 &
  local wpid=$!
  if ! echo "$wpid" > "$WATCH_DIR/watch-${wpid}.pid" 2>/dev/null; then
    kill "$wpid" 2>/dev/null || true
  fi
}

# 既存ペインに agent を起動(0.7.5 方式)。作成直後は agent_pane_busy になるためリトライ。
start_agent_in_pane() {
  local name="$1" pane_id="$2" prompt="$3"
  local args=("$name" --kind "$AGENT_CMD" --pane "$pane_id")
  [ -n "$prompt" ] && args+=(-- "$prompt")
  local started=0 start_err="" _try
  for _try in $(seq 1 30); do
    if start_err="$(herdr agent start "${args[@]}" 2>&1 >/dev/null)"; then started=1; break; fi
    sleep 0.5
  done
  if [ "$started" -ne 1 ]; then
    notify "hwt: $name の起動に失敗しました (pane $pane_id)" request
    echo "エラー: agent start が失敗しました: $(printf '%s' "$start_err" | tail -1)" >&2
    return 1
  fi
}

usage() {
  cat <<'EOF'
hwt — herdr worktree を楽に扱う

  hwt new [-n N] [テキスト]      新規 worktree+workspace(agent なし)
  hwt new -a [-n N] [テキスト]   新規 worktree+workspace + agent 起動
  hwt ls                        現 repo の hwt/* worktree を一覧
  hwt cd                        worktree の workspace を選んで移動
  hwt clean                     hwt/* の残骸を安全に掃除(変更ありは保護)
  hwt rm                        今いる worktree を確認つき破棄
EOF
}

# ---- verb: new ----
cmd_new() {
  local with_agent=0 count=1 text=""
  while [ $# -gt 0 ]; do
    case "$1" in
      -a|--agent) with_agent=1 ;;
      -n) shift; count="${1:-1}" ;;
      -h|--help) echo "usage: hwt new [-a] [-n N] [テキスト]"; return 0 ;;
      -*) echo "hwt new: 不明なオプション $1" >&2; return 1 ;;
      *) text="${text:+$text }$1" ;;
    esac
    shift
  done
  if ! [[ "$count" =~ ^[0-9]+$ ]] || [ "$count" -lt 1 ] || [ "$count" -gt 8 ]; then
    echo "エラー: -n は 1〜8 で指定してください" >&2; return 1
  fi
  local cwd; cwd="$(resolve_cwd)"
  if ! git -C "$cwd" rev-parse --show-toplevel >/dev/null 2>&1; then
    notify "hwt: $cwd は git リポジトリではありません" request
    echo "エラー: $cwd は git リポジトリではありません" >&2; return 1
  fi
  local stamp slug agent_slug
  stamp="$(date +%Y%m%d-%H%M%S)"
  slug="$(slugify "$text")"; [ -z "$slug" ] && slug="hwt"
  agent_slug="$(printf '%s' "$slug" | tr 'A-Z' 'a-z' | tr -cs 'a-z0-9' '-' | sed 's/^-*//; s/-*$//' | cut -c1-18 | sed 's/-*$//')"
  case "$agent_slug" in [a-z]*) ;; *) agent_slug="h${agent_slug}" ;; esac
  [ -z "$agent_slug" ] && agent_slug="hwt"

  local launched=() i name branch focus_opt wt_json ws_id wt_path pane_id
  for i in $(seq 1 "$count"); do
    if [ "$count" -gt 1 ]; then name="${agent_slug}-${stamp}-${i}"; branch="hwt/${stamp}-${slug}-${i}"
    else name="${agent_slug}-${stamp}"; branch="hwt/${stamp}-${slug}"; fi
    focus_opt="--no-focus"; [ "$count" -eq 1 ] && focus_opt="--focus"
    wt_json="$(herdr worktree create --cwd "$cwd" --branch "$branch" --label "$name" "$focus_opt" --json | grep -m1 '^{')"
    ws_id="$(printf '%s' "$wt_json" | jq -r '.result.workspace.workspace_id // empty')"
    wt_path="$(printf '%s' "$wt_json" | jq -r '.result.workspace.worktree.checkout_path // .result.worktree.path // empty')"
    pane_id="$(printf '%s' "$wt_json" | jq -r '.result.root_pane.pane_id // empty')"
    if [ -z "$ws_id" ] || [ -z "$wt_path" ] || [ -z "$pane_id" ]; then
      notify "hwt: worktree 作成に失敗しました" request
      echo "エラー: worktree 作成結果を解析できません: $wt_json" >&2; return 1
    fi
    echo "→ worktree: $wt_path (branch $branch, workspace $ws_id)"
    if [ "$with_agent" -eq 1 ]; then
      start_agent_in_pane "$name" "$pane_id" "$text" || return 1
      watch_agent "$name"
      launched+=("$name")
    fi
  done
  if [ "$with_agent" -eq 1 ]; then
    echo ""; echo "起動完了: ${launched[*]}"
    echo "状態確認: hwt ls / herdr サイドバー。idle/blocked で通知します。"
  fi
}

# ---- verb ディスパッチ ----
[ $# -eq 0 ] && { usage; exit 0; }
verb="$1"; shift
case "$verb" in
  new)       cmd_new "$@" ;;
  -h|--help) usage ;;
  *)         echo "hwt: 不明なコマンド '$verb'" >&2; usage >&2; exit 1 ;;
esac
```

- [ ] **Step 2: 構文チェック**

Run: `bash -n plugins/hwt/hwt.sh && echo OK`
Expected: `OK`

- [ ] **Step 3: unknown verb と usage を検証**

Run: `bash plugins/hwt/hwt.sh 2>&1 | head -1`
Expected: `hwt — herdr worktree を楽に扱う`

Run: `bash plugins/hwt/hwt.sh bogus 2>&1 | head -1`
Expected: `hwt: 不明なコマンド 'bogus'`

- [ ] **Step 4: `new`（agent なし）を E2E 検証**

```bash
rm -rf /tmp/hwt-t1 && git init -q /tmp/hwt-t1 && (cd /tmp/hwt-t1 && git commit -q --allow-empty -m init)
(cd /tmp/hwt-t1 && bash ~/ghq/github.com/s-hiraoku/herdr-toolkit/plugins/hwt/hwt.sh new)
```
Expected: `→ worktree: …/hwt-t1/… (branch hwt/<日時>, workspace <id>)` が出て、
`git -C /tmp/hwt-t1 worktree list` に `hwt/<日時>` の worktree が現れる。agent は起動しない。

- [ ] **Step 5: `new -a`（agent 起動）を E2E 検証**

```bash
(cd /tmp/hwt-t1 && bash ~/ghq/github.com/s-hiraoku/herdr-toolkit/plugins/hwt/hwt.sh new -a "test prompt")
sleep 5
herdr agent list 2>/dev/null | grep -m1 '^{' | jq -r '.result.agents[]?.agent_status' | head
```
Expected: 起動完了メッセージが出て、agent が idle 等で現れる。

- [ ] **Step 6: テスト残骸を掃除**

```bash
herdr worktree list --cwd /tmp/hwt-t1 --json 2>/dev/null | grep -m1 '^{' \
  | jq -r '.result.worktrees[]?.open_workspace_id // empty' | while read -r w; do
    [ -n "$w" ] && herdr worktree remove --workspace "$w" --force >/dev/null 2>&1; done
rm -rf /tmp/hwt-t1 ~/.herdr/worktrees/hwt-t1
```

- [ ] **Step 7: Commit**

```bash
git add plugins/hwt/hwt.sh
git commit -m "feat(hwt): verb ディスパッチと new(worktree/-a agent)を実装。local 廃止・hwt/* 接頭辞・HWT_ 環境変数へ"
```

---

### Task 3: `clean` verb

**Files:** Modify `plugins/hwt/hwt.sh`

- [ ] **Step 1: `cmd_clean` 関数を `cmd_new` の後（dispatch の前）に追加**

```bash
# ---- verb: clean ----
cmd_clean() {
  local cwd repo_root; cwd="$(resolve_cwd)"
  if ! repo_root="$(git -C "$cwd" rev-parse --show-toplevel 2>/dev/null)"; then
    notify "hwt clean: $cwd は git リポジトリではありません" request
    echo "エラー: $cwd は git リポジトリではありません" >&2; return 1
  fi
  local removed=0 kept=0 kept_list="" open_map
  open_map="$(herdr worktree list --cwd "$repo_root" --json 2>/dev/null | grep -m1 '^{' \
    | jq -r '.result.worktrees[]? | select(.open_workspace_id != null) | "\(.path)\t\(.open_workspace_id)"' 2>/dev/null || true)"
  local wt_path wt_branch dirty unique ws_id reason
  while IFS=$'\t' read -r wt_path wt_branch; do
    [ -z "$wt_path" ] && continue
    dirty="$(git -C "$wt_path" status --porcelain 2>/dev/null | head -1)"
    unique="$(git -C "$repo_root" rev-list --count "$wt_branch" --not --exclude="$wt_branch" --branches --remotes 2>/dev/null || echo 999)"
    if [ -z "$dirty" ] && [ "$unique" = "0" ]; then
      ws_id="$(printf '%s\n' "$open_map" | awk -F'\t' -v p="$wt_path" '$1==p {print $2}')"
      [ -n "$ws_id" ] && herdr worktree remove --workspace "$ws_id" --force >/dev/null 2>&1 || true
      git -C "$repo_root" worktree remove --force "$wt_path" >/dev/null 2>&1 || true
      git -C "$repo_root" worktree prune >/dev/null 2>&1 || true
      git -C "$repo_root" branch -D "$wt_branch" >/dev/null 2>&1 || true
      echo "削除: $wt_branch ($wt_path)"; removed=$((removed + 1))
    else
      reason=""; [ -n "$dirty" ] && reason="未コミットの変更あり"
      [ "$unique" != "0" ] && reason="${reason:+$reason / }独自コミット ${unique} 件"
      echo "保護: $wt_branch — $reason"; kept=$((kept + 1)); kept_list="${kept_list:+$kept_list, }$wt_branch"
    fi
  done < <(git -C "$repo_root" worktree list --porcelain \
    | awk '/^worktree /{p=$2} /^branch refs\/heads\/hwt\//{sub("refs/heads/","",$2); print p "\t" $2}')
  echo ""; echo "掃除完了: 削除 ${removed} 件 / 保護 ${kept} 件"
  notify "hwt clean: 削除 ${removed} / 保護 ${kept}${kept_list:+ ($kept_list)}" done
}
```

- [ ] **Step 2: dispatch の case に `clean` を追加**

`case "$verb" in` の `new) …` 行の下に追加:

```bash
  clean)     cmd_clean "$@" ;;
```

- [ ] **Step 3: 構文チェック**

Run: `bash -n plugins/hwt/hwt.sh && echo OK`
Expected: `OK`

- [ ] **Step 4: clean の保護/削除判定を E2E 検証**

```bash
rm -rf /tmp/hwt-t3 && git init -q /tmp/hwt-t3 && (cd /tmp/hwt-t3 && git commit -q --allow-empty -m init)
DSH=~/ghq/github.com/s-hiraoku/herdr-toolkit/plugins/hwt/hwt.sh
(cd /tmp/hwt-t3 && bash "$DSH" new)          # 変更なし worktree(削除対象)
(cd /tmp/hwt-t3 && bash "$DSH" new)
# 片方に変更を作る
wt=$(git -C /tmp/hwt-t3 worktree list --porcelain | awk '/^worktree /{p=$2} /hwt\//{print p; exit}')
touch "$wt/dirty.txt"
(cd /tmp/hwt-t3 && bash "$DSH" clean)
```
Expected: 「削除: hwt/… / 保護: hwt/…（未コミットの変更あり）」が出て、変更ありの 1 本だけ残る。

- [ ] **Step 5: 残骸掃除**

```bash
herdr worktree list --cwd /tmp/hwt-t3 --json 2>/dev/null | grep -m1 '^{' \
  | jq -r '.result.worktrees[]?.open_workspace_id // empty' | while read -r w; do
    [ -n "$w" ] && herdr worktree remove --workspace "$w" --force >/dev/null 2>&1; done
rm -rf /tmp/hwt-t3 ~/.herdr/worktrees/hwt-t3
```

- [ ] **Step 6: Commit**

```bash
git add plugins/hwt/hwt.sh
git commit -m "feat(hwt): clean verb(hwt/* の安全一括掃除)を実装"
```

---

### Task 4: `rm` verb

**Files:** Modify `plugins/hwt/hwt.sh`

- [ ] **Step 1: `cmd_rm` 関数を追加**

```bash
# ---- verb: rm ----
cmd_rm() {
  local cwd br wt_root; cwd="$(resolve_cwd)"
  br="$(git -C "$cwd" branch --show-current 2>/dev/null || true)"
  wt_root="$(git -C "$cwd" rev-parse --show-toplevel 2>/dev/null || true)"
  case "$br" in
    hwt/*) ;;
    *)
      echo "ここは hwt worktree ではありません (branch: ${br:-なし})"
      echo "誤爆防止のため、hwt/* ブランチの worktree でのみ使えます。"
      echo "Enter で閉じる..."; read -r _; return 1 ;;
  esac
  local main_git_dir main_root dirty_count unique ans ws_id
  main_git_dir="$(git -C "$wt_root" rev-parse --git-common-dir)"
  main_root="$(dirname "$main_git_dir")"
  echo "破棄対象: $wt_root"; echo "ブランチ: $br"
  dirty_count="$(git -C "$wt_root" status --porcelain | wc -l | tr -d ' ')"
  unique="$(git -C "$main_root" rev-list --count "$br" --not --exclude="$br" --branches --remotes 2>/dev/null || echo '?')"
  echo "未コミットの変更: ${dirty_count} ファイル / 独自コミット: ${unique} 件"
  if [ "$dirty_count" != "0" ]; then
    echo "--- 変更ファイル ---"; git -C "$wt_root" status --porcelain | head -10
  fi
  echo ""; printf "この worktree・ブランチ・workspace を完全に破棄しますか? [y/N] "
  read -r ans
  case "$ans" in y|Y|yes) ;; *) echo "中止しました"; return 0 ;; esac
  ws_id="$(herdr worktree list --cwd "$main_root" --json 2>/dev/null | grep -m1 '^{' \
    | jq -r --arg p "$wt_root" '.result.worktrees[]? | select(.path == $p) | .open_workspace_id // empty')"
  [ -n "$ws_id" ] && herdr worktree remove --workspace "$ws_id" --force >/dev/null 2>&1 || true
  git -C "$main_root" worktree remove --force "$wt_root" >/dev/null 2>&1 || true
  git -C "$main_root" worktree prune >/dev/null 2>&1 || true
  git -C "$main_root" branch -D "$br" >/dev/null 2>&1 || true
  notify "hwt: $br を破棄しました" done
}
```

- [ ] **Step 2: dispatch の case に `rm` を追加**

```bash
  rm)        cmd_rm "$@" ;;
```

- [ ] **Step 3: 構文チェック**

Run: `bash -n plugins/hwt/hwt.sh && echo OK`
Expected: `OK`

- [ ] **Step 4: 誤爆防止（hwt/* 以外では拒否）を検証**

```bash
rm -rf /tmp/hwt-t4 && git init -q /tmp/hwt-t4 && (cd /tmp/hwt-t4 && git commit -q --allow-empty -m init)
echo n | (cd /tmp/hwt-t4 && bash ~/ghq/github.com/s-hiraoku/herdr-toolkit/plugins/hwt/hwt.sh rm) 2>&1 | head -1
```
Expected: `ここは hwt worktree ではありません (branch: main)` （main では実行を拒否）

- [ ] **Step 5: hwt worktree 内での破棄を検証**

```bash
DSH=~/ghq/github.com/s-hiraoku/herdr-toolkit/plugins/hwt/hwt.sh
(cd /tmp/hwt-t4 && bash "$DSH" new)
wt=$(git -C /tmp/hwt-t4 worktree list --porcelain | awk '/^worktree /{p=$2} /hwt\//{print p; exit}')
echo y | (cd "$wt" && bash "$DSH" rm) 2>&1 | tail -2
git -C /tmp/hwt-t4 worktree list | grep -c 'hwt/' || echo 0
```
Expected: 破棄メッセージが出て、`hwt/` worktree が 0 になる。

- [ ] **Step 6: 残骸掃除**

```bash
rm -rf /tmp/hwt-t4 ~/.herdr/worktrees/hwt-t4
```

- [ ] **Step 7: Commit**

```bash
git add plugins/hwt/hwt.sh
git commit -m "feat(hwt): rm verb(今いる worktree を確認破棄・hwt/* 限定)を実装"
```

---

### Task 5: `ls` verb（新規）

**Files:** Modify `plugins/hwt/hwt.sh`

**Interfaces（想定する herdr JSON フィールド。実装 Step で実出力に照合し必要なら jq パスを修正）:**
- `herdr worktree list --json` → `.result.worktrees[] | {path, open_workspace_id}`
- `herdr agent list --json` → `.result.agents[] | {pane_id, agent_status}`（workspace は `pane_id` の
  `:` 前が workspace_id）

- [ ] **Step 1: herdr の実 JSON を確認して jq パスを確定**

```bash
herdr worktree list --json 2>/dev/null | grep -m1 '^{' | jq '.result.worktrees[0]'
herdr agent list --json 2>/dev/null | grep -m1 '^{' | jq '.result.agents[0]'
```
Expected: `worktrees[].path` と `.open_workspace_id`、`agents[].pane_id`/`.agent_status` が存在する
ことを確認（無ければ以下の `cmd_ls` の jq パスを実出力に合わせて修正する）。

- [ ] **Step 2: `cmd_ls` 関数を追加**

```bash
# ---- verb: ls ----
cmd_ls() {
  local cwd repo_root; cwd="$(resolve_cwd)"
  if ! repo_root="$(git -C "$cwd" rev-parse --show-toplevel 2>/dev/null)"; then
    echo "エラー: $cwd は git リポジトリではありません" >&2; return 1
  fi
  local wt_list agent_list
  wt_list="$(herdr worktree list --cwd "$repo_root" --json 2>/dev/null | grep -m1 '^{')"
  agent_list="$(herdr agent list --json 2>/dev/null | grep -m1 '^{' || true)"
  printf '%-30s %-11s %-9s %s\n' "BRANCH" "WORKSPACE" "AGENT" "PATH"
  local wt_path wt_branch ws_id st
  while IFS=$'\t' read -r wt_path wt_branch; do
    [ -z "$wt_path" ] && continue
    ws_id="$(printf '%s' "$wt_list" | jq -r --arg p "$wt_path" '.result.worktrees[]? | select(.path==$p) | .open_workspace_id // "-"')"
    st="-"
    if [ "$ws_id" != "-" ] && [ -n "$ws_id" ] && [ -n "$agent_list" ]; then
      st="$(printf '%s' "$agent_list" | jq -r --arg w "$ws_id" '[.result.agents[]? | select((.pane_id // "") | startswith($w + ":"))][0].agent_status // "-"' 2>/dev/null || echo '-')"
    fi
    printf '%-30s %-11s %-9s %s\n' "$wt_branch" "${ws_id:--}" "$st" "$wt_path"
  done < <(git -C "$repo_root" worktree list --porcelain \
    | awk '/^worktree /{p=$2} /^branch refs\/heads\/hwt\//{sub("refs/heads/","",$2); print p "\t" $2}')
}
```

- [ ] **Step 3: dispatch の case に `ls` を追加**

```bash
  ls)        cmd_ls "$@" ;;
```

- [ ] **Step 4: 構文チェック**

Run: `bash -n plugins/hwt/hwt.sh && echo OK`
Expected: `OK`

- [ ] **Step 5: ls の表出力を E2E 検証**

```bash
rm -rf /tmp/hwt-t5 && git init -q /tmp/hwt-t5 && (cd /tmp/hwt-t5 && git commit -q --allow-empty -m init)
DSH=~/ghq/github.com/s-hiraoku/herdr-toolkit/plugins/hwt/hwt.sh
(cd /tmp/hwt-t5 && bash "$DSH" new >/dev/null)
(cd /tmp/hwt-t5 && bash "$DSH" ls)
```
Expected: ヘッダ行 `BRANCH  WORKSPACE  AGENT  PATH` と、作成した `hwt/<日時>` の行が表示される。

- [ ] **Step 6: 残骸掃除**

```bash
herdr worktree list --cwd /tmp/hwt-t5 --json 2>/dev/null | grep -m1 '^{' \
  | jq -r '.result.worktrees[]?.open_workspace_id // empty' | while read -r w; do
    [ -n "$w" ] && herdr worktree remove --workspace "$w" --force >/dev/null 2>&1; done
rm -rf /tmp/hwt-t5 ~/.herdr/worktrees/hwt-t5
```

- [ ] **Step 7: Commit**

```bash
git add plugins/hwt/hwt.sh
git commit -m "feat(hwt): ls verb(現 repo の hwt/* worktree を状態付きで一覧)を実装"
```

---

### Task 6: `cd` verb（新規）

**Files:** Modify `plugins/hwt/hwt.sh`

- [ ] **Step 1: `cmd_cd` 関数を追加**

```bash
# ---- verb: cd (workspace へ移動) ----
cmd_cd() {
  local cwd repo_root; cwd="$(resolve_cwd)"
  if ! repo_root="$(git -C "$cwd" rev-parse --show-toplevel 2>/dev/null)"; then
    echo "エラー: $cwd は git リポジトリではありません" >&2; return 1
  fi
  local wt_list rows sel ws_id n
  wt_list="$(herdr worktree list --cwd "$repo_root" --json 2>/dev/null | grep -m1 '^{')"
  rows="$(git -C "$repo_root" worktree list --porcelain \
    | awk '/^worktree /{p=$2} /^branch refs\/heads\/hwt\//{sub("refs/heads/","",$2); print p "\t" $2}' \
    | while IFS=$'\t' read -r p b; do
        wid="$(printf '%s' "$wt_list" | jq -r --arg p "$p" '.result.worktrees[]? | select(.path==$p) | .open_workspace_id // empty')"
        [ -n "$wid" ] && printf '%s\t%s\n' "$wid" "$b"
      done)"
  if [ -z "$rows" ]; then echo "移動できる hwt worktree の workspace がありません"; return 0; fi
  if command -v fzf >/dev/null 2>&1; then
    sel="$(printf '%s\n' "$rows" | fzf --delimiter='\t' --with-nth=2)"
  else
    printf '%s\n' "$rows" | awk -F'\t' '{printf "  %d) %s\n", NR, $2}'
    printf "> "; read -r n
    sel="$(printf '%s\n' "$rows" | sed -n "${n}p")"
  fi
  ws_id="$(printf '%s' "$sel" | cut -f1)"
  [ -n "$ws_id" ] && herdr workspace focus "$ws_id"
}
```

- [ ] **Step 2: dispatch の case に `cd` を追加**

```bash
  cd)        cmd_cd "$@" ;;
```

- [ ] **Step 3: 構文チェック**

Run: `bash -n plugins/hwt/hwt.sh && echo OK`
Expected: `OK`

- [ ] **Step 4: cd の選択→focus を E2E 検証（番号選択パス）**

```bash
rm -rf /tmp/hwt-t6 && git init -q /tmp/hwt-t6 && (cd /tmp/hwt-t6 && git commit -q --allow-empty -m init)
DSH=~/ghq/github.com/s-hiraoku/herdr-toolkit/plugins/hwt/hwt.sh
(cd /tmp/hwt-t6 && bash "$DSH" new >/dev/null)
# fzf を使わない番号選択パスを検証(PATH から fzf を外して 1 を選ぶ)
echo 1 | (cd /tmp/hwt-t6 && PATH=/usr/bin:/bin bash "$DSH" cd) 2>&1 | tail -2
```
Expected: 候補 `1) hwt/<日時>` が出て、選ぶと `herdr workspace focus` が実行される（エラーなく終了）。

- [ ] **Step 5: 残骸掃除**

```bash
herdr worktree list --cwd /tmp/hwt-t6 --json 2>/dev/null | grep -m1 '^{' \
  | jq -r '.result.worktrees[]?.open_workspace_id // empty' | while read -r w; do
    [ -n "$w" ] && herdr worktree remove --workspace "$w" --force >/dev/null 2>&1; done
rm -rf /tmp/hwt-t6 ~/.herdr/worktrees/hwt-t6
```

- [ ] **Step 6: Commit**

```bash
git add plugins/hwt/hwt.sh
git commit -m "feat(hwt): cd verb(worktree の workspace を選んで focus 移動)を実装"
```

---

### Task 7: config.toml のキーバインド

**Files:** Modify `config/config.toml`

- [ ] **Step 1: dispatch のキーバインドブロックを hwt に置換**

`config/config.toml` の dispatch プラグイン節（`# dispatch プラグイン (herdr-toolkit)` から
`prefix+X`/`prefix+shift+x` の `[[keys.command]]` まで）を以下に置き換える。既存の
`agent_panel_sort = "priority"` 行は残す。

```toml
# ─────────────────────────────────────────────
# hwt プラグイン (herdr-toolkit) — herdr worktree ラッパー
# プラグイン本体: https://github.com/s-hiraoku/herdr-toolkit (plugins/hwt)
# ─────────────────────────────────────────────
agent_panel_sort = "priority"

[[keys.command]]
key = "prefix+shift+d"
type = "plugin_action"
command = "s-hiraoku.hwt.new-agent"
description = "hwt: 新規 worktree + agent 起動(旧 prefix+shift+d 同等)"

[[keys.command]]
key = "prefix+d"
type = "plugin_action"
command = "s-hiraoku.hwt.new"
description = "hwt: 新規 worktree(agent なし)"

[[keys.command]]
key = "prefix+shift+c"
type = "plugin_action"
command = "s-hiraoku.hwt.clean"
description = "hwt: hwt/* の残骸を安全掃除(変更ありは保護)"

[[keys.command]]
key = "prefix+shift+x"
type = "popup"
command = '''bash "$HOME/ghq/github.com/s-hiraoku/herdr-toolkit/plugins/hwt/hwt.sh" rm'''
description = "hwt: 今いる worktree を確認つき破棄"
```

注: `rm` は確認プロンプトを出すため TTY が要る。plugin action は非対話なので、旧 `--discard`
と同じく `type = "popup"` で **shell コマンド文字列**として `hwt.sh rm` を叩く（plugin action id
は指定しない）。`ls`/`cd`/`rm` は TTY 前提で、キー/CLI からは動くが `herdr plugin action invoke`
（非対話）では対話部分が働かない点に注意。

- [ ] **Step 2: 設定を検証・リロード**

Run: `herdr config check`
Expected: `config: ok`

Run: `herdr server reload-config`
Expected: JSON に `"status":"applied"`、`diagnostics` が空

- [ ] **Step 3: 各 action がキー経由（plugin action）で呼べることを確認**

```bash
herdr plugin action invoke new --plugin s-hiraoku.hwt >/dev/null 2>&1; echo "new: $?"
herdr plugin action invoke clean --plugin s-hiraoku.hwt >/dev/null 2>&1; echo "clean: $?"
```
Expected: いずれも `0`（対象 repo は herdr のフォーカス pane cwd。無ければ git エラーで非0でも
可＝action 解決自体は成立している。ここでは action が存在し呼べることの確認）。

- [ ] **Step 4: Commit**

```bash
git add config/config.toml
git commit -m "feat(hwt): config のキーバインドを hwt へ(prefix+shift+d=new-agent, prefix+d=new)"
```

---

### Task 8: setup.sh の汎用化（複数プラグイン対応＋CLI を PATH へ）

**Files:** Modify `scripts/setup.sh`

- [ ] **Step 1: setup.sh を以下に書き換え**

```bash
#!/usr/bin/env bash
# 新マシン/更新時のセットアップ:
#   1. home-manager switch (nix-config 側で config.toml のリンクが張られる)
#   2. このスクリプトで plugins/* を herdr に link し、CLI を PATH に通す
set -eu

REPO_DIR="$(cd "$(dirname "$0")/.." && pwd)"
BIN_DIR="${HWT_BIN_DIR:-$HOME/.local/bin}"

if ! command -v herdr >/dev/null 2>&1; then
  echo "herdr が見つかりません。brew install herdr を先に実行してください" >&2
  exit 1
fi

mkdir -p "$BIN_DIR"

for manifest in "$REPO_DIR"/plugins/*/herdr-plugin.toml; do
  [ -e "$manifest" ] || continue
  plugin_dir="$(dirname "$manifest")"
  name="$(basename "$plugin_dir")"
  echo "→ link plugin: $name"
  herdr plugin link "$plugin_dir" >/dev/null 2>&1 || herdr plugin link "$plugin_dir" || true
  # CLI を公開するプラグイン(<name>.sh を持つ)は PATH に symlink
  if [ -f "$plugin_dir/$name.sh" ]; then
    ln -sf "$plugin_dir/$name.sh" "$BIN_DIR/$name"
    echo "  CLI: $BIN_DIR/$name → $plugin_dir/$name.sh"
  fi
done

herdr plugin list | grep -iE "$(basename "$REPO_DIR")|hwt" || true
echo "done. PATH に $BIN_DIR を通してください（未設定なら shell rc に追加）。"
echo "      config.toml のリンクは nix-config (home-manager switch) 側で管理されます。"
```

- [ ] **Step 2: setup.sh を実行して検証**

```bash
bash ~/ghq/github.com/s-hiraoku/herdr-toolkit/scripts/setup.sh
ls -l ~/.local/bin/hwt
herdr plugin list | grep -i hwt
```
Expected: `~/.local/bin/hwt` が `plugins/hwt/hwt.sh` への symlink になっており、
plugin list に `s-hiraoku.hwt` が出る。

- [ ] **Step 3: PATH 経由で hwt が引けることを確認**

```bash
PATH="$HOME/.local/bin:$PATH" hwt 2>&1 | head -1
```
Expected: `hwt — herdr worktree を楽に扱う`

- [ ] **Step 4: Commit**

```bash
git add scripts/setup.sh
git commit -m "feat(setup): plugins/* を汎用 link し CLI を ~/.local/bin へ symlink"
```

---

### Task 9: README（トップ + plugins/hwt/README.md）

**Files:** Modify `README.md`、Rewrite `plugins/hwt/README.md`

- [ ] **Step 1: `plugins/hwt/README.md` を verb 表記で書き換え**

以下の内容にする（操作リファレンス表を含む）:

````markdown
# hwt

git worktree を短いコマンドで扱う herdr ラッパー。内部で `herdr worktree` を叩く。
AI エージェント起動は `new -a` の付加機能。

## インストール

```bash
herdr plugin link ~/ghq/github.com/s-hiraoku/herdr-toolkit/plugins/hwt
# CLI を PATH に通す(setup.sh が ~/.local/bin/hwt を張る)
```

## 操作リファレンス

| CLI | plugin action | keybinding | 説明 |
| --- | --- | --- | --- |
| `hwt new [-n N] [テキスト]` | `new` | `prefix+d` | 新規 worktree+workspace(agent なし) |
| `hwt new -a [-n N] [テキスト]` | `new-agent` | `prefix+shift+d` | 新規 worktree+workspace + agent 起動 |
| `hwt ls` | `ls` | （なし） | 現 repo の `hwt/*` worktree を状態付き一覧 |
| `hwt cd` | `cd` | （なし） | worktree の workspace を選んで移動 |
| `hwt clean` | `clean` | `prefix+shift+c` | `hwt/*` の残骸を安全掃除(変更ありは保護) |
| `hwt rm` | `rm` | `prefix+shift+x` | 今いる worktree を確認つき破棄 |

`hwt new`/`new -a` は worktree を新しい workspace として開き、親 repo の workspace に
グループ化し、フォーカスを移す。`-a` 時は位置引数を agent の初期プロンプトにも使う。

## 環境変数

| 変数 | 既定 | 説明 |
| --- | --- | --- |
| `HWT_AGENT` | `claude` | `-a` で起動する agent |
| `HWT_MAX_WATCHERS` | `16` | 完了監視の同時上限(best-effort) |

## 開発メモ

- herdr `--json` は先頭に `ok` 等が混ざる → `grep -m1 '^{'` で抽出
- worktree パスは `.result.workspace.worktree.checkout_path`
- `git rev-list --exclude` は `refs/heads/` を除いた短い名前で渡す
- macOS 同梱 bash は 3.2。`$BASHPID` 等は使わない(watcher の PID は親側 `$!`)
- plugin action の `$PWD` はプラグイン root。対象 repo は `HERDR_PLUGIN_CONTEXT_JSON`
  の `focused_pane_cwd` から解決。herdr 本体は `HERDR_BIN_PATH` で PATH 補完
````

- [ ] **Step 2: トップ `README.md` を更新**

トップ README の「構成」ツリーと「各プラグインへのリンク」を hwt に更新する。
`plugins/dispatch/` の記述を `plugins/hwt/` に、リンクを `[hwt](plugins/hwt/README.md)` に変える。
（PR #3 マージ済みなら既にプラグイン分割済みなので、dispatch→hwt の置換のみ。未マージなら
トップ README を「toolkit 概要＋plugins/hwt へのリンク」の最小構成に整える。）

- [ ] **Step 3: リンク先の存在を検証**

Run: `test -f plugins/hwt/README.md && grep -q 'plugins/hwt/README.md' README.md && echo OK`
Expected: `OK`

- [ ] **Step 4: 旧 dispatch 表記が残っていないか確認**

Run: `grep -rn 'dispatch' README.md plugins/hwt/README.md config/config.toml || echo "残存なし"`
Expected: `残存なし`（もし出たら旧表記を hwt に修正）

- [ ] **Step 5: Commit**

```bash
git add README.md plugins/hwt/README.md
git commit -m "docs(hwt): README を hwt の verb 表記に更新"
```

---

## Self-Review

- **Spec coverage:** リポジトリ構造(Task 8) / コマンドモデル new・ls・cd・clean・rm(Task 2-6) /
  ショートカット(Task 7) / workspace 操作感(Task 2 cmd_new の --focus) / ハードカット移行
  (Task 1・7・9) / bash 3.2・HERDR_BIN_PATH・watcher 上限(Task 2) — spec 各節に対応タスクあり。
- **Placeholder scan:** 各 code step に実コードあり。ls/cd の jq フィールドのみ Task 5 Step 1 で
  実出力照合の手順を明示（外部 CLI 連携のため。プレースホルダではなく検証手順）。
- **Type/名前整合:** 共有関数 `resolve_cwd`/`slugify`/`notify`/`watch_agent`/`start_agent_in_pane`
  は Task 2 で定義し Task 3-6 が参照。action id（new/new-agent/ls/cd/clean/rm）は manifest(Task 1)・
  config(Task 7)・README(Task 9)で一致。ブランチ接頭辞 `hwt/` は全 verb で統一。
